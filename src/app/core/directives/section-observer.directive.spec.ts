import { SectionObserverDirective } from './section-observer.directive';

describe('SectionObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionObserverDirective();
    expect(directive).toBeTruthy();
  });
});
